# FFmpeg Plugin Manifest

name: FFmpeg
version: "7.1.0"
mandatory: false
enabled: false

source:
  type: url
  base_uri: https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-win64-gpl.zip

command:
  path: bin
  executable: ffmpeg.exe

register_to_path: true

dependencies: []

description: Complete solution to record, convert and stream audio and video
