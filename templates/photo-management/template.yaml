name: Photo Management Template
description: Photography workflow with RAW processing, selections, and metadata organization
version: 1.0.0
min_app_version: 0.0.0
extends: default
required_plugins:
  - exiftool
  - imagemagick
  - digikam
folder_structure:
  - media
  - exports
  - cache
  - raw
  - processed
  - selections
  - metadata
migration_notes: {}
